version: '3'

services:
  svc:
    image: ${TestGenerateComposefileEnv_IMAGE}
    build:
      context: ${TestGenerateComposefileEnv_CONTEXT}
      dockerfile: ${TestGenerateComposefileEnv_DOCKERFILE}